<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.11.0" xml:lang="en-US">
  <compounddef id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e" kind="page">
    <compoundname>md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e</compoundname>
    <title>safe-buffer &lt;a href=&quot;https://travis-ci.org/feross/safe-buffer&quot; &gt;&lt;img src=&quot;https://img.shields.io/travis/feross/safe-buffer/master.svg&quot; alt=&quot;travis&quot;/&gt;&lt;/a&gt; &lt;a href=&quot;https://npmjs.org/package/safe-buffer&quot; &gt;&lt;img src=&quot;https://img.shields.io/npm/v/safe-buffer.svg&quot; alt=&quot;npm&quot;/&gt;&lt;/a&gt; &lt;a href=&quot;https://npmjs.org/package/safe-buffer&quot; &gt;&lt;img src=&quot;https://img.shields.io/npm/dm/safe-buffer.svg&quot; alt=&quot;downloads&quot;/&gt;&lt;/a&gt; &lt;a href=&quot;https://standardjs.com&quot; &gt;&lt;img src=&quot;https://img.shields.io/badge/code_style-standard-brightgreen.svg&quot; alt=&quot;javascript style guide&quot;/&gt;&lt;/a&gt;</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><anchor id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3016"/></para>
<sect3 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3017">
<title>Safer Node.js Buffer API</title><para>**Use the new Node.js Buffer APIs (<computeroutput>Buffer.from</computeroutput>, <computeroutput>Buffer.alloc</computeroutput>, <computeroutput>Buffer.allocUnsafe</computeroutput>, <computeroutput>Buffer.allocUnsafeSlow</computeroutput>) in all versions of Node.js.**</para>
<para><bold>Uses the built-in implementation when available.</bold></para>
</sect3>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3018">
<title>install</title><para><programlisting><codeline><highlight class="normal">npm<sp/>install<sp/>safe-buffer</highlight></codeline>
</programlisting></para>
</sect1>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3019">
<title>usage</title><para>The goal of this package is to provide a safe replacement for the node.js <computeroutput>Buffer</computeroutput>.</para>
<para>It&apos;s a drop-in replacement for <computeroutput>Buffer</computeroutput>. You can use it by adding one <computeroutput>require</computeroutput> line to the top of your node.js modules:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">var<sp/>Buffer<sp/>=<sp/>require(&apos;safe-buffer&apos;).Buffer</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Existing<sp/>buffer<sp/>code<sp/>will<sp/>continue<sp/>to<sp/>work<sp/>without<sp/>issues:</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">new<sp/>Buffer(&apos;hey&apos;,<sp/>&apos;utf8&apos;)</highlight></codeline>
<codeline><highlight class="normal">new<sp/>Buffer([1,<sp/>2,<sp/>3],<sp/>&apos;utf8&apos;)</highlight></codeline>
<codeline><highlight class="normal">new<sp/>Buffer(obj)</highlight></codeline>
<codeline><highlight class="normal">new<sp/>Buffer(16)<sp/>//<sp/>create<sp/>an<sp/>uninitialized<sp/>buffer<sp/>(potentially<sp/>unsafe)</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>But<sp/>you<sp/>can<sp/>use<sp/>these<sp/>new<sp/>explicit<sp/>APIs<sp/>to<sp/>make<sp/>clear<sp/>what<sp/>you<sp/>want:</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">Buffer.from(&apos;hey&apos;,<sp/>&apos;utf8&apos;)<sp/>//<sp/>convert<sp/>from<sp/>many<sp/>types<sp/>to<sp/>a<sp/>Buffer</highlight></codeline>
<codeline><highlight class="normal">Buffer.alloc(16)<sp/>//<sp/>create<sp/>a<sp/>zero-filled<sp/>buffer<sp/>(safe)</highlight></codeline>
<codeline><highlight class="normal">Buffer.allocUnsafe(16)<sp/>//<sp/>create<sp/>an<sp/>uninitialized<sp/>buffer<sp/>(potentially<sp/>unsafe)</highlight></codeline>
</programlisting></para>
</sect1>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3020">
<title>api</title><sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3021">
<title>Class Method: Buffer.from(array)</title><para><itemizedlist>
<listitem><para><computeroutput>array</computeroutput> {Array}</para>
</listitem></itemizedlist>
</para>
<para>Allocates a new <computeroutput>Buffer</computeroutput> using an <computeroutput>array</computeroutput> of octets.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>buf<sp/>=<sp/>Buffer.from([0x62,0x75,0x66,0x66,0x65,0x72]);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>creates<sp/>a<sp/>new<sp/>Buffer<sp/>containing<sp/>ASCII<sp/>bytes</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>[&apos;b&apos;,&apos;u&apos;,&apos;f&apos;,&apos;f&apos;,&apos;e&apos;,&apos;r&apos;]</highlight></codeline>
</programlisting></para>
<para>A <computeroutput>TypeError</computeroutput> will be thrown if <computeroutput>array</computeroutput> is not an <computeroutput>Array</computeroutput>.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3022">
<title>Class Method: Buffer.from(arrayBuffer[, byteOffset[, length]])</title><para><itemizedlist>
<listitem><para><computeroutput>arrayBuffer</computeroutput> {ArrayBuffer} The <computeroutput>.buffer</computeroutput> property of a <computeroutput>TypedArray</computeroutput> or a <computeroutput>new ArrayBuffer()</computeroutput></para>
</listitem><listitem><para><computeroutput>byteOffset</computeroutput> {Number} Default: <computeroutput>0</computeroutput></para>
</listitem><listitem><para><computeroutput>length</computeroutput> {Number} Default: <computeroutput>arrayBuffer.length - byteOffset</computeroutput></para>
</listitem></itemizedlist>
</para>
<para>When passed a reference to the <computeroutput>.buffer</computeroutput> property of a <computeroutput>TypedArray</computeroutput> instance, the newly created <computeroutput>Buffer</computeroutput> will share the same allocated memory as the TypedArray.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>arr<sp/>=<sp/>new<sp/>Uint16Array(2);</highlight></codeline>
<codeline><highlight class="normal">arr[0]<sp/>=<sp/>5000;</highlight></codeline>
<codeline><highlight class="normal">arr[1]<sp/>=<sp/>4000;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">const<sp/>buf<sp/>=<sp/>Buffer.from(arr.buffer);<sp/>//<sp/>shares<sp/>the<sp/>memory<sp/>with<sp/>arr;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">console.log(buf);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Prints:<sp/>&lt;Buffer<sp/>88<sp/>13<sp/>a0<sp/>0f&gt;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>changing<sp/>the<sp/>TypedArray<sp/>changes<sp/>the<sp/>Buffer<sp/>also</highlight></codeline>
<codeline><highlight class="normal">arr[1]<sp/>=<sp/>6000;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">console.log(buf);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Prints:<sp/>&lt;Buffer<sp/>88<sp/>13<sp/>70<sp/>17&gt;</highlight></codeline>
</programlisting></para>
<para>The optional <computeroutput>byteOffset</computeroutput> and <computeroutput>length</computeroutput> arguments specify a memory range within the <computeroutput>arrayBuffer</computeroutput> that will be shared by the <computeroutput>Buffer</computeroutput>.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>ab<sp/>=<sp/>new<sp/>ArrayBuffer(10);</highlight></codeline>
<codeline><highlight class="normal">const<sp/>buf<sp/>=<sp/>Buffer.from(ab,<sp/>0,<sp/>2);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf.length);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Prints:<sp/>2</highlight></codeline>
</programlisting></para>
<para>A <computeroutput>TypeError</computeroutput> will be thrown if <computeroutput>arrayBuffer</computeroutput> is not an <computeroutput>ArrayBuffer</computeroutput>.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3023">
<title>Class Method: Buffer.from(buffer)</title><para><itemizedlist>
<listitem><para><computeroutput>buffer</computeroutput> {Buffer}</para>
</listitem></itemizedlist>
</para>
<para>Copies the passed <computeroutput>buffer</computeroutput> data onto a new <computeroutput>Buffer</computeroutput> instance.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>buf1<sp/>=<sp/>Buffer.from(&apos;buffer&apos;);</highlight></codeline>
<codeline><highlight class="normal">const<sp/>buf2<sp/>=<sp/>Buffer.from(buf1);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">buf1[0]<sp/>=<sp/>0x61;</highlight></codeline>
<codeline><highlight class="normal">console.log(buf1.toString());</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>&apos;auffer&apos;</highlight></codeline>
<codeline><highlight class="normal">console.log(buf2.toString());</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>&apos;buffer&apos;<sp/>(copy<sp/>is<sp/>not<sp/>changed)</highlight></codeline>
</programlisting></para>
<para>A <computeroutput>TypeError</computeroutput> will be thrown if <computeroutput>buffer</computeroutput> is not a <computeroutput>Buffer</computeroutput>.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3024">
<title>Class Method: Buffer.from(str[, encoding])</title><para><itemizedlist>
<listitem><para><computeroutput>str</computeroutput> {String} String to encode.</para>
</listitem><listitem><para><computeroutput>encoding</computeroutput> {String} Encoding to use, Default: <lsquo/>&apos;utf8<rsquo/>`</para>
</listitem></itemizedlist>
</para>
<para>Creates a new <computeroutput>Buffer</computeroutput> containing the given JavaScript string <computeroutput>str</computeroutput>. If provided, the <computeroutput>encoding</computeroutput> parameter identifies the character encoding. If not provided, <computeroutput>encoding</computeroutput> defaults to <lsquo/>&apos;utf8<rsquo/>`.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>buf1<sp/>=<sp/>Buffer.from(&apos;this<sp/>is<sp/>a<sp/>tést&apos;);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf1.toString());</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>prints:<sp/>this<sp/>is<sp/>a<sp/>tést</highlight></codeline>
<codeline><highlight class="normal">console.log(buf1.toString(&apos;ascii&apos;));</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>prints:<sp/>this<sp/>is<sp/>a<sp/>tC)st</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">const<sp/>buf2<sp/>=<sp/>Buffer.from(&apos;7468697320697320612074c3a97374&apos;,<sp/>&apos;hex&apos;);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf2.toString());</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>prints:<sp/>this<sp/>is<sp/>a<sp/>tést</highlight></codeline>
</programlisting></para>
<para>A <computeroutput>TypeError</computeroutput> will be thrown if <computeroutput>str</computeroutput> is not a string.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3025">
<title>Class Method: Buffer.alloc(size[, fill[, encoding]])</title><para><itemizedlist>
<listitem><para><computeroutput>size</computeroutput> {Number}</para>
</listitem><listitem><para><computeroutput>fill</computeroutput> {Value} Default: <computeroutput>undefined</computeroutput></para>
</listitem><listitem><para><computeroutput>encoding</computeroutput> {String} Default: <computeroutput>utf8</computeroutput></para>
</listitem></itemizedlist>
</para>
<para>Allocates a new <computeroutput>Buffer</computeroutput> of <computeroutput>size</computeroutput> bytes. If <computeroutput>fill</computeroutput> is <computeroutput>undefined</computeroutput>, the <computeroutput>Buffer</computeroutput> will be <emphasis>zero-filled</emphasis>.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>buf<sp/>=<sp/>Buffer.alloc(5);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>&lt;Buffer<sp/>00<sp/>00<sp/>00<sp/>00<sp/>00&gt;</highlight></codeline>
</programlisting></para>
<para>The <computeroutput>size</computeroutput> must be less than or equal to the value of <lsquo/>require(&apos;buffer<rsquo/>).kMaxLength<computeroutput>(on 64-bit architectures,</computeroutput>kMaxLength<computeroutput>is </computeroutput>(2^31)-1<computeroutput>). Otherwise, a [</computeroutput>RangeError<computeroutput>][] is thrown. A zero-length Buffer will be created if a</computeroutput>size` less than or equal to 0 is specified.</para>
<para>If <computeroutput>fill</computeroutput> is specified, the allocated <computeroutput>Buffer</computeroutput> will be initialized by calling <computeroutput>buf.fill(fill)</computeroutput>. See [<computeroutput>buf.fill()</computeroutput>][] for more information.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>buf<sp/>=<sp/>Buffer.alloc(5,<sp/>&apos;a&apos;);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>&lt;Buffer<sp/>61<sp/>61<sp/>61<sp/>61<sp/>61&gt;</highlight></codeline>
</programlisting></para>
<para>If both <computeroutput>fill</computeroutput> and <computeroutput>encoding</computeroutput> are specified, the allocated <computeroutput>Buffer</computeroutput> will be initialized by calling <computeroutput>buf.fill(fill, encoding)</computeroutput>. For example:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>buf<sp/>=<sp/>Buffer.alloc(11,<sp/>&apos;aGVsbG8gd29ybGQ=&apos;,<sp/>&apos;base64&apos;);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>&lt;Buffer<sp/>68<sp/>65<sp/>6c<sp/>6c<sp/>6f<sp/>20<sp/>77<sp/>6f<sp/>72<sp/>6c<sp/>64&gt;</highlight></codeline>
</programlisting></para>
<para>Calling <computeroutput>Buffer.alloc(size)</computeroutput> can be significantly slower than the alternative <computeroutput>Buffer.allocUnsafe(size)</computeroutput> but ensures that the newly created <computeroutput>Buffer</computeroutput> instance contents will <emphasis>never contain sensitive data</emphasis>.</para>
<para>A <computeroutput>TypeError</computeroutput> will be thrown if <computeroutput>size</computeroutput> is not a number.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3026">
<title>Class Method: Buffer.allocUnsafe(size)</title><para><itemizedlist>
<listitem><para><computeroutput>size</computeroutput> {Number}</para>
</listitem></itemizedlist>
</para>
<para>Allocates a new <emphasis>non-zero-filled</emphasis> <computeroutput>Buffer</computeroutput> of <computeroutput>size</computeroutput> bytes. The <computeroutput>size</computeroutput> must be less than or equal to the value of <lsquo/>require(&apos;buffer<rsquo/>).kMaxLength<computeroutput>(on 64-bit architectures,</computeroutput>kMaxLength<computeroutput>is</computeroutput>(2^31)-1<computeroutput>). Otherwise, a [</computeroutput>RangeError<computeroutput>][] is thrown. A zero-length Buffer will be created if a</computeroutput>size` less than or equal to 0 is specified.</para>
<para>The underlying memory for <computeroutput>Buffer</computeroutput> instances created in this way is <emphasis>not initialized</emphasis>. The contents of the newly created <computeroutput>Buffer</computeroutput> are unknown and <emphasis>may contain sensitive data</emphasis>. Use [<computeroutput>buf.fill(0)</computeroutput>][] to initialize such <computeroutput>Buffer</computeroutput> instances to zeroes.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">const<sp/>buf<sp/>=<sp/>Buffer.allocUnsafe(5);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>&lt;Buffer<sp/>78<sp/>e0<sp/>82<sp/>02<sp/>01&gt;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>(octets<sp/>will<sp/>be<sp/>different,<sp/>every<sp/>time)</highlight></codeline>
<codeline><highlight class="normal">buf.fill(0);</highlight></codeline>
<codeline><highlight class="normal">console.log(buf);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>&lt;Buffer<sp/>00<sp/>00<sp/>00<sp/>00<sp/>00&gt;</highlight></codeline>
</programlisting></para>
<para>A <computeroutput>TypeError</computeroutput> will be thrown if <computeroutput>size</computeroutput> is not a number.</para>
<para>Note that the <computeroutput>Buffer</computeroutput> module pre-allocates an internal <computeroutput>Buffer</computeroutput> instance of size <computeroutput>Buffer.poolSize</computeroutput> that is used as a pool for the fast allocation of new <computeroutput>Buffer</computeroutput> instances created using <computeroutput>Buffer.allocUnsafe(size)</computeroutput> (and the deprecated <computeroutput>new Buffer(size)</computeroutput> constructor) only when <computeroutput>size</computeroutput> is less than or equal to <computeroutput>Buffer.poolSize &gt;&gt; 1</computeroutput> (floor of <computeroutput>Buffer.poolSize</computeroutput> divided by two). The default value of <computeroutput>Buffer.poolSize</computeroutput> is <computeroutput>8192</computeroutput> but can be modified.</para>
<para>Use of this pre-allocated internal memory pool is a key difference between calling <computeroutput>Buffer.alloc(size, fill)</computeroutput> vs. <computeroutput>Buffer.allocUnsafe(size).fill(fill)</computeroutput>. Specifically, <computeroutput>Buffer.alloc(size, fill)</computeroutput> will <emphasis>never</emphasis> use the internal Buffer pool, while <computeroutput>Buffer.allocUnsafe(size).fill(fill)</computeroutput> <emphasis>will</emphasis> use the internal Buffer pool if <computeroutput>size</computeroutput> is less than or equal to half <computeroutput>Buffer.poolSize</computeroutput>. The difference is subtle but can be important when an application requires the additional performance that <computeroutput>Buffer.allocUnsafe(size)</computeroutput> provides.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3027">
<title>Class Method: Buffer.allocUnsafeSlow(size)</title><para><itemizedlist>
<listitem><para><computeroutput>size</computeroutput> {Number}</para>
</listitem></itemizedlist>
</para>
<para>Allocates a new <emphasis>non-zero-filled</emphasis> and non-pooled <computeroutput>Buffer</computeroutput> of <computeroutput>size</computeroutput> bytes. The <computeroutput>size</computeroutput> must be less than or equal to the value of <lsquo/>require(&apos;buffer<rsquo/>).kMaxLength<computeroutput>(on 64-bit architectures,</computeroutput>kMaxLength<computeroutput>is </computeroutput>(2^31)-1<computeroutput>). Otherwise, a [</computeroutput>RangeError<computeroutput>][] is thrown. A zero-length Buffer will be created if a</computeroutput>size` less than or equal to 0 is specified.</para>
<para>The underlying memory for <computeroutput>Buffer</computeroutput> instances created in this way is <emphasis>not initialized</emphasis>. The contents of the newly created <computeroutput>Buffer</computeroutput> are unknown and <emphasis>may contain sensitive data</emphasis>. Use [<computeroutput>buf.fill(0)</computeroutput>][] to initialize such <computeroutput>Buffer</computeroutput> instances to zeroes.</para>
<para>When using <computeroutput>Buffer.allocUnsafe()</computeroutput> to allocate new <computeroutput>Buffer</computeroutput> instances, allocations under 4KB are, by default, sliced from a single pre-allocated <computeroutput>Buffer</computeroutput>. This allows applications to avoid the garbage collection overhead of creating many individually allocated Buffers. This approach improves both performance and memory usage by eliminating the need to track and cleanup as many <computeroutput>Persistent</computeroutput> objects.</para>
<para>However, in the case where a developer may need to retain a small chunk of memory from a pool for an indeterminate amount of time, it may be appropriate to create an un-pooled Buffer instance using <computeroutput>Buffer.allocUnsafeSlow()</computeroutput> then copy out the relevant bits.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">//<sp/>need<sp/>to<sp/>keep<sp/>around<sp/>a<sp/>few<sp/>small<sp/>chunks<sp/>of<sp/>memory</highlight></codeline>
<codeline><highlight class="normal">const<sp/>store<sp/>=<sp/>[];</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">socket.on(&apos;readable&apos;,<sp/>()<sp/>=&gt;<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>const<sp/>data<sp/>=<sp/>socket.read();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>allocate<sp/>for<sp/>retained<sp/>data</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>const<sp/>sb<sp/>=<sp/>Buffer.allocUnsafeSlow(10);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>copy<sp/>the<sp/>data<sp/>into<sp/>the<sp/>new<sp/>allocation</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>data.copy(sb,<sp/>0,<sp/>0,<sp/>10);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>store.push(sb);</highlight></codeline>
<codeline><highlight class="normal">});</highlight></codeline>
</programlisting></para>
<para>Use of <computeroutput>Buffer.allocUnsafeSlow()</computeroutput> should be used only as a last resort <emphasis>after</emphasis> a developer has observed undue memory retention in their applications.</para>
<para>A <computeroutput>TypeError</computeroutput> will be thrown if <computeroutput>size</computeroutput> is not a number.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3028">
<title>All the Rest</title><para>The rest of the <computeroutput>Buffer</computeroutput> API is exactly the same as in node.js. <ulink url="https://nodejs.org/api/buffer.html">See the docs</ulink>.</para>
</sect2>
</sect1>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3029">
<title>Related links</title><para><itemizedlist>
<listitem><para><ulink url="https://github.com/nodejs/node/issues/4660">Node.js issue: Buffer(number) is unsafe</ulink></para>
</listitem><listitem><para><ulink url="https://github.com/nodejs/node-eps/pull/4">Node.js Enhancement Proposal: Buffer.from/Buffer.alloc/Buffer.zalloc/Buffer() soft-deprecate</ulink></para>
</listitem></itemizedlist>
</para>
</sect1>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3030">
<title>Why is <computeroutput>Buffer</computeroutput> unsafe?</title><para>Today, the node.js <computeroutput>Buffer</computeroutput> constructor is overloaded to handle many different argument types like <computeroutput>String</computeroutput>, <computeroutput>Array</computeroutput>, <computeroutput>Object</computeroutput>, <computeroutput>TypedArrayView</computeroutput> (<computeroutput>Uint8Array</computeroutput>, etc.), <computeroutput>ArrayBuffer</computeroutput>, and also <computeroutput>Number</computeroutput>.</para>
<para>The API is optimized for convenience: you can throw any type at it, and it will try to do what you want.</para>
<para>Because the Buffer constructor is so powerful, you often see code like this:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">//<sp/>Convert<sp/>UTF-8<sp/>strings<sp/>to<sp/>hex</highlight></codeline>
<codeline><highlight class="normal">function<sp/>toHex<sp/>(str)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>return<sp/>new<sp/>Buffer(str).toString(&apos;hex&apos;)</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para>
<para><emphasis><bold>But what happens if <computeroutput>toHex</computeroutput> is called with a <computeroutput>Number</computeroutput> argument?</bold></emphasis></para>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3031">
<title>Remote Memory Disclosure</title><para>If an attacker can make your program call the <computeroutput>Buffer</computeroutput> constructor with a <computeroutput>Number</computeroutput> argument, then they can make it allocate uninitialized memory from the node.js process. This could potentially disclose TLS private keys, user data, or database passwords.</para>
<para>When the <computeroutput>Buffer</computeroutput> constructor is passed a <computeroutput>Number</computeroutput> argument, it returns an <bold>UNINITIALIZED</bold> block of memory of the specified <computeroutput>size</computeroutput>. When you create a <computeroutput>Buffer</computeroutput> like this, you <bold>MUST</bold> overwrite the contents before returning it to the user.</para>
<para>From the <ulink url="https://nodejs.org/api/buffer.html#buffer_new_buffer_size">node.js docs</ulink>:</para>
<para><blockquote><para><zwj/><computeroutput>new Buffer(size)</computeroutput></para>
<para><itemizedlist>
<listitem><para><computeroutput>size</computeroutput> Number</para>
</listitem></itemizedlist>
</para>
<para>The underlying memory for <computeroutput>Buffer</computeroutput> instances created in this way is not initialized. <bold>The contents of a newly created <computeroutput>Buffer</computeroutput> are unknown and could contain sensitive data.</bold> Use <computeroutput>buf.fill(0)</computeroutput> to initialize a Buffer to zeroes. </para>
</blockquote></para>
<para>(Emphasis our own.)</para>
<para>Whenever the programmer intended to create an uninitialized <computeroutput>Buffer</computeroutput> you often see code like this:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">var<sp/>buf<sp/>=<sp/>new<sp/>Buffer(16)</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Immediately<sp/>overwrite<sp/>the<sp/>uninitialized<sp/>buffer<sp/>with<sp/>data<sp/>from<sp/>another<sp/>buffer</highlight></codeline>
<codeline><highlight class="normal">for<sp/>(var<sp/>i<sp/>=<sp/>0;<sp/>i<sp/>&lt;<sp/>buf.length;<sp/>i++)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>buf[i]<sp/>=<sp/>otherBuf[i]</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3032">
<title>Would this ever be a problem in real code?</title><para>Yes. It&apos;s surprisingly common to forget to check the type of your variables in a dynamically-typed language like JavaScript.</para>
<para>Usually the consequences of assuming the wrong type is that your program crashes with an uncaught exception. But the failure mode for forgetting to check the type of arguments to the <computeroutput>Buffer</computeroutput> constructor is more catastrophic.</para>
<para>Here&apos;s an example of a vulnerable service that takes a JSON payload and converts it to hex:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">//<sp/>Take<sp/>a<sp/>JSON<sp/>payload<sp/>{str:<sp/>&quot;some<sp/>string&quot;}<sp/>and<sp/>convert<sp/>it<sp/>to<sp/>hex</highlight></codeline>
<codeline><highlight class="normal">var<sp/>server<sp/>=<sp/>http.createServer(function<sp/>(req,<sp/>res)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>var<sp/>data<sp/>=<sp/>&apos;&apos;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>req.setEncoding(&apos;utf8&apos;)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>req.on(&apos;data&apos;,<sp/>function<sp/>(chunk)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>data<sp/>+=<sp/>chunk</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>})</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>req.on(&apos;end&apos;,<sp/>function<sp/>()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>var<sp/>body<sp/>=<sp/>JSON.parse(data)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>res.end(new<sp/>Buffer(body.str).toString(&apos;hex&apos;))</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>})</highlight></codeline>
<codeline><highlight class="normal">})</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">server.listen(8080)</highlight></codeline>
</programlisting></para>
<para>In this example, an http client just has to send:</para>
<para><programlisting filename=".json"><codeline><highlight class="normal">{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>&quot;str&quot;:<sp/>1000</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para>
<para>and it will get back 1,000 bytes of uninitialized memory from the server.</para>
<para>This is a very serious bug. It&apos;s similar in severity to the <ulink url="http://heartbleed.com/">the Heartbleed bug</ulink> that allowed disclosure of OpenSSL process memory by remote attackers.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3033">
<title>Which real-world packages were vulnerable?</title><sect3 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3034">
<title><ulink url="https://www.npmjs.com/package/bittorrent-dht"><computeroutput>bittorrent-dht</computeroutput></ulink></title><para><ulink url="https://github.com/mafintosh">Mathias Buus</ulink> and I (<ulink url="http://feross.org/">Feross Aboukhadijeh</ulink>) found this issue in one of our own packages, <ulink url="https://www.npmjs.com/package/bittorrent-dht"><computeroutput>bittorrent-dht</computeroutput></ulink>. The bug would allow anyone on the internet to send a series of messages to a user of <computeroutput>bittorrent-dht</computeroutput> and get them to reveal 20 bytes at a time of uninitialized memory from the node.js process.</para>
<para>Here&apos;s <ulink url="https://github.com/feross/bittorrent-dht/commit/6c7da04025d5633699800a99ec3fbadf70ad35b8">the commit</ulink> that fixed it. We released a new fixed version, created a <ulink url="https://nodesecurity.io/advisories/68">Node Security Project disclosure</ulink>, and deprecated all vulnerable versions on npm so users will get a warning to upgrade to a newer version.</para>
</sect3>
<sect3 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3035">
<title><ulink url="https://www.npmjs.com/package/ws"><computeroutput>ws</computeroutput></ulink></title><para>That got us wondering if there were other vulnerable packages. Sure enough, within a short period of time, we found the same issue in <ulink url="https://www.npmjs.com/package/ws"><computeroutput>ws</computeroutput></ulink>, the most popular WebSocket implementation in node.js.</para>
<para>If certain APIs were called with <computeroutput>Number</computeroutput> parameters instead of <computeroutput>String</computeroutput> or <computeroutput>Buffer</computeroutput> as expected, then uninitialized server memory would be disclosed to the remote peer.</para>
<para>These were the vulnerable methods:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">socket.send(number)</highlight></codeline>
<codeline><highlight class="normal">socket.ping(number)</highlight></codeline>
<codeline><highlight class="normal">socket.pong(number)</highlight></codeline>
</programlisting></para>
<para>Here&apos;s a vulnerable socket server with some echo functionality:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">server.on(&apos;connection&apos;,<sp/>function<sp/>(socket)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>socket.on(&apos;message&apos;,<sp/>function<sp/>(message)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>message<sp/>=<sp/>JSON.parse(message)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>if<sp/>(message.type<sp/>===<sp/>&apos;echo&apos;)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>socket.send(message.data)<sp/>//<sp/>send<sp/>back<sp/>the<sp/>user&apos;s<sp/>message</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>})</highlight></codeline>
<codeline><highlight class="normal">})</highlight></codeline>
</programlisting></para>
<para><computeroutput>socket.send(number)</computeroutput> called on the server, will disclose server memory.</para>
<para>Here&apos;s <ulink url="https://github.com/websockets/ws/releases/tag/1.0.1">the release</ulink> where the issue was fixed, with a more detailed explanation. Props to <ulink url="https://github.com/3rd-Eden">Arnout Kazemier</ulink> for the quick fix. Here&apos;s the <ulink url="https://nodesecurity.io/advisories/67">Node Security Project disclosure</ulink>.</para>
</sect3>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3036">
<title>What&apos;s the solution?</title><para>It&apos;s important that node.js offers a fast way to get memory otherwise performance-critical applications would needlessly get a lot slower.</para>
<para>But we need a better way to <emphasis>signal our intent</emphasis> as programmers. <bold>When we want uninitialized memory, we should request it explicitly.</bold></para>
<para>Sensitive functionality should not be packed into a developer-friendly API that loosely accepts many different types. This type of API encourages the lazy practice of passing variables in without checking the type very carefully.</para>
<sect3 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3037">
<title>A new API: <computeroutput>Buffer.allocUnsafe(number)</computeroutput></title><para>The functionality of creating buffers with uninitialized memory should be part of another API. We propose <computeroutput>Buffer.allocUnsafe(number)</computeroutput>. This way, it&apos;s not part of an API that frequently gets user input of all sorts of different types passed into it.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">var<sp/>buf<sp/>=<sp/>Buffer.allocUnsafe(16)<sp/>//<sp/>careful,<sp/>uninitialized<sp/>memory!</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Immediately<sp/>overwrite<sp/>the<sp/>uninitialized<sp/>buffer<sp/>with<sp/>data<sp/>from<sp/>another<sp/>buffer</highlight></codeline>
<codeline><highlight class="normal">for<sp/>(var<sp/>i<sp/>=<sp/>0;<sp/>i<sp/>&lt;<sp/>buf.length;<sp/>i++)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>buf[i]<sp/>=<sp/>otherBuf[i]</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para>
</sect3>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3038">
<title>How do we fix node.js core?</title><para>We sent <ulink url="https://github.com/nodejs/node/pull/4514">a PR to node.js core</ulink> (merged as <computeroutput>semver-major</computeroutput>) which defends against one case:</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">var<sp/>str<sp/>=<sp/>16</highlight></codeline>
<codeline><highlight class="normal">new<sp/>Buffer(str,<sp/>&apos;utf8&apos;)</highlight></codeline>
</programlisting></para>
<para>In this situation, it&apos;s implied that the programmer intended the first argument to be a string, since they passed an encoding as a second argument. Today, node.js will allocate uninitialized memory in the case of <computeroutput>new Buffer(number, encoding)</computeroutput>, which is probably not what the programmer intended.</para>
<para>But this is only a partial solution, since if the programmer does <computeroutput>new Buffer(variable)</computeroutput> (without an <computeroutput>encoding</computeroutput> parameter) there&apos;s no way to know what they intended. If <computeroutput>variable</computeroutput> is sometimes a number, then uninitialized memory will sometimes be returned.</para>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3039">
<title>What&apos;s the real long-term fix?</title><para>We could deprecate and remove <computeroutput>new Buffer(number)</computeroutput> and use <computeroutput>Buffer.allocUnsafe(number)</computeroutput> when we need uninitialized memory. But that would break 1000s of packages.</para>
<para><strike>We believe the best solution is to:</strike></para>
<para><strike>1. Change <computeroutput>new Buffer(number)</computeroutput> to return safe, zeroed-out memory</strike></para>
<para><strike>2. Create a new API for creating uninitialized Buffers. We propose: <computeroutput>Buffer.allocUnsafe(number)</computeroutput></strike></para>
<sect3 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3040">
<title>Update</title><para>We now support adding three new APIs:</para>
<para><itemizedlist>
<listitem><para><computeroutput>Buffer.from(value)</computeroutput> - convert from any type to a buffer</para>
</listitem><listitem><para><computeroutput>Buffer.alloc(size)</computeroutput> - create a zero-filled buffer</para>
</listitem><listitem><para><computeroutput>Buffer.allocUnsafe(size)</computeroutput> - create an uninitialized buffer with given size</para>
</listitem></itemizedlist>
</para>
<para>This solves the core problem that affected <computeroutput>ws</computeroutput> and <computeroutput>bittorrent-dht</computeroutput> which is <computeroutput>Buffer(variable)</computeroutput> getting tricked into taking a number argument.</para>
<para>This way, existing code continues working and the impact on the npm ecosystem will be minimal. Over time, npm maintainers can migrate performance-critical code to use <computeroutput>Buffer.allocUnsafe(number)</computeroutput> instead of <computeroutput>new Buffer(number)</computeroutput>.</para>
</sect3>
</sect2>
<sect2 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3041">
<title>Conclusion</title><para>We think there&apos;s a serious design issue with the <computeroutput>Buffer</computeroutput> API as it exists today. It promotes insecure software by putting high-risk functionality into a convenient API with friendly &quot;developer ergonomics&quot;.</para>
<para>This wasn&apos;t merely a theoretical exercise because we found the issue in some of the most popular npm packages.</para>
<para>Fortunately, there&apos;s an easy fix that can be applied today. Use <computeroutput>safe-buffer</computeroutput> in place of <computeroutput>buffer</computeroutput>.</para>
<para><programlisting filename=".js"><codeline><highlight class="normal">var<sp/>Buffer<sp/>=<sp/>require(&apos;safe-buffer&apos;).Buffer</highlight></codeline>
</programlisting></para>
<para>Eventually, we hope that node.js core can switch to this new, safer behavior. We believe the impact on the ecosystem would be minimal since it&apos;s not a breaking change. Well-maintained, popular packages would be updated to use <computeroutput>Buffer.alloc</computeroutput> quickly, while older, insecure packages would magically become safe from this attack vector.</para>
</sect2>
</sect1>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3042">
<title>links</title><para><itemizedlist>
<listitem><para><ulink url="https://github.com/nodejs/node/pull/4514">Node.js PR: buffer: throw if both length and enc are passed</ulink></para>
</listitem><listitem><para><ulink url="https://nodesecurity.io/advisories/67">Node Security Project disclosure for <computeroutput>ws</computeroutput></ulink></para>
</listitem><listitem><para><ulink url="https://nodesecurity.io/advisories/68">Node Security Project disclosure for<computeroutput>bittorrent-dht</computeroutput></ulink></para>
</listitem></itemizedlist>
</para>
</sect1>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3043">
<title>credit</title><para>The original issues in <computeroutput>bittorrent-dht</computeroutput> (<ulink url="https://nodesecurity.io/advisories/68">disclosure</ulink>) and <computeroutput>ws</computeroutput> (<ulink url="https://nodesecurity.io/advisories/67">disclosure</ulink>) were discovered by <ulink url="https://github.com/mafintosh">Mathias Buus</ulink> and <ulink url="http://feross.org/">Feross Aboukhadijeh</ulink>.</para>
<para>Thanks to <ulink url="https://github.com/evilpacket">Adam Baldwin</ulink> for helping disclose these issues and for his work running the <ulink url="https://nodesecurity.io/">Node Security Project</ulink>.</para>
<para>Thanks to <ulink url="https://github.com/jhiesey">John Hiesey</ulink> for proofreading this README and auditing the code.</para>
</sect1>
<sect1 id="md_backend_2node__modules_2safe-buffer_2_r_e_a_d_m_e_1autotoc_md3044">
<title>license</title><para>MIT. Copyright (C) <ulink url="http://feross.org">Feross Aboukhadijeh</ulink> </para>
</sect1>
    </detaileddescription>
    <location file="backend/node_modules/safe-buffer/README.md"/>
  </compounddef>
</doxygen>
